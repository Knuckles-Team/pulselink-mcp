[pytest]
timeout = 60
asyncio_mode = auto
testpaths = tests
markers =
    integration: Integration tests
    live: Tests requiring live external network (real sources); skipped by default
addopts = -m "not integration and not live"
filterwarnings =
    ignore:.*exclude_args.*
